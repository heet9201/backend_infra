service: backend-infra

runtime: python310
entrypoint: uvicorn main:app --host 0.0.0.0 --port $PORT

env_variables:
  GOOGLE_APPLICATION_CREDENTIALS: "gs://purva-api_cloudbuild/source/purva-api-74d1474dc39b.json"

handlers:
  - url: /static
    static_dir: static
  - url: /.*
    script: auto
